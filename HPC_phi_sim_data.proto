syntax = "proto3";

package SavedSimulations;

message SimulationData {

    message ParameterFieldEntry {
        optional string key = 1;
        optional float value = 2;
    }

    repeated ParameterFieldEntry parameter = 1;
    repeated float spike_phi = 2;

}